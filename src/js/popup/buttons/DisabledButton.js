import Button from './Button'
import v from 'css/variables'

export default Button.extend`
  background-color: ${v.darkenWhite};
  border: 2px solid ${v.lightenGrey};
  box-shadow: none;
  cursor: default;
`
